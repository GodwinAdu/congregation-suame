import BibleReadingClient from "./_components/bible-reading-client"

export default function BibleReadingPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50">
      <BibleReadingClient />
    </div>
  )
}
